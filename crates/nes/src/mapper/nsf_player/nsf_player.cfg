MEMORY {
    ZP:         start = $0000, size = $100, type = rw;
    ROM:        start = $5400, size = $400, fill = yes, file = %O;
    RAM:        start = $5800, size = $400, fill = no;
}

SEGMENTS {
    ZEROPAGE:   load = ZP, type = zp;
    CODE:       load = ROM, type = ro;
    BSS:        load = RAM, type = bss;
}
